class AppState {
  const AppState._();

  factory AppState.initialize() {
    return const AppState._();
  }

  AppState copyWith() {
    return const AppState._();
  }

  @override
  String toString() => 'AppState{'
      '}';
}
